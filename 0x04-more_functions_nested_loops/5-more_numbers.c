#include "main.h"
#include <stdio.h>

/**
 * more_numbers - check code
 * 
 * Return: no return.
 */
void more_numbers(void)
{
	int a, b;

	for (a = 0; a <= 9; a++)
	{
		for (b = 0; b <= 14; b++)
		{
			_putchar(b);
		}
	_putchar('\n')
	}
}

