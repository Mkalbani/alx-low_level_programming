#include "main.h"
#include <stdio.h>

/**
 * _isdigit - check the code.
 * @a: multiplies integers
 * @b: mulitplies integers
 * Return: Always 0.
 */
int mul(int a, int b)
{
	printf(%d\n: mul(a, b);
	return (0);
}
