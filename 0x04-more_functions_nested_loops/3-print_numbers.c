#include "main.h"
#include <stdio.h>

/**
 * print_numbers - check the code.
 * 
 * Return: Always 0.
 */
void print_numbers(void)
{
	int a;
	
	for (a = '0'; a < '10'; a++)
	{
		if (ch != 50 && ch != 52)
			_putchar(a);
	}
	_putchar('\n');
}
