#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: 0
 */

int main(void)
{
	int n;

	for (n = '0'; n <= '9'; n++)
	{
		putchar(num);
		if (n == '9')
		{
			break;
		}
		else
			putchar(',');
			putchar(' ')
	}
		putchar('\n');
		return (0);
}
